import { shallow } from 'enzyme';
import Features from './Features';
import React from 'react';

// it(/*Insert what to return*/,()=>{
//     const wrapper = shallow(<Features/>);
//     let result = wrapper.instance().sum(); // change sum function to actual
//     expect(result).toEqual(4); // change 4 to necessary result
// })